# Agent Development Rules (High-Speed Vibe Coding Mode)

## 1. Core Philosophy (핵심 원칙)
- **Fast Feedback Loop**: 불필요한 사전 마크다운 문서 작성 단계를 생략하고, '즉시 탐색 -> 소단위 수정 -> 자동 테스트 자가 검증 -> 결과 보고' 루프로 신속하게 작업한다.
- **Zero-Bug Safety Guardrails**: 개발 속도를 극대화하되, 아래 5대 안전 수칙을 엄격히 준수하여 회귀 버그를 사전에 차단한다.
- **Token Efficiency (RTK Setup)**: 토큰 소비 절감을 위해 셸 명령어 실행 시 `rtk` 접두사(`rtk git status`, `rtk pytest` 등)를 적극 활용한다.

## 2. [CRITICAL] 5대 고속 바이브 코딩 안전 수칙 (Core Guardrails)

1. **Targeted Windowing Protocol (토큰 최적화 및 속도 향상)**:
   - 300줄 이상의 대용량 파일은 전체를 한 번에 조회하지 않는다.
   - 반드시 `rg` (ripgrep)로 타겟 라인 번호($N$)를 먼저 파악한 후, $N \pm 50$ 줄 범위로 제한하여 조회한다.
2. **Zero-Guessing API Contract (인터페이스 추측 금지)**:
   - 함수 시그니처, Pydantic 스키마 모델, 프론트엔드 모듈 인터페이스를 자의적으로 추측하여 작성하지 않는다.
   - 반드시 원본 정의를 `rg`로 확인한 후 호출 코드를 작성한다.
3. **Automated Self-Verification (자동 자가 검증)**:
   - 로직 또는 API 변경 직후 반드시 `pytest` 단위 테스트나 검증 스크립트를 자동 실행한다.
   - 테스트 결과가 통과(Exit Code 0)하기 전에는 작업을 임의로 완료로 선언하지 않는다.
4. **Incremental Small Diffs (소단위 점진적 수정)**:
   - 거대 파일의 무분별한 전체 덮어쓰기나 무리한 대규모 리팩터링을 지양하고, 명확하고 작은 diff 단위로 점진 수정한다.
5. **Clean Code & Lean Comments (간결한 주석과 타입 힌트)**:
   - **타입 힌트 우선**: 함수 시그니처에 명확한 타입 힌트(`Type Annotations`)를 필수로 작성하여 모호성을 없앤다.
   - **Why 중심의 주석**: 코드 자체로 자명한 설명성 주석은 생략하고, 비즈니스 의도(`Why`)나 특이 제약/엣지 케이스만 1줄로 간결히 기록한다.
   - **주석 최신화 유지**: 코드를 수정할 때 기존 주석이 불일치하면 즉시 수정하거나 삭제하여 환각을 방지한다.

## 3. Workflow & Approval Policy (작업 절차)
- **일반 기능 구현 및 버그 수정**: 별도의 사전 문서 작성 없이 즉시 탐색 -> 소단위 코드 수정 -> 자동 검증 -> 결과 보고 순서로 빠르게 진행한다.
- **대규모 아키텍처 개편/DB 마이그레이션**: 사용자가 명시적으로 요구할 때만 핵심 요약 계획을 대화창에서 간결히 제시하고 확인을 받는다.

## 4. Git & Branching Rules (버전 관리)
- `main` 또는 `master` 브랜치에 직접 푸시하지 않는다.
- 커밋 메시지는 한글을 기본으로 작성하며, 변경 목적과 검증 결과를 명확히 기록한다.
- `archive/` 폴더는 사용자의 명시적 요청이 없는 한 기본 탐색 및 컨텍스트 수집에서 제외한다.
