# [개발 가이드] Subcast 다중 디스플레이 출력 및 소프트웨어 분산 연동 설계서

본 문서는 실시간 자막/슬라이드 송출 시스템인 **Subcast**를 다중 PC 및 듀얼 모니터 환경에 세팅하고, 개발 효율성을 극대화하기 위해 하드웨어 제어 장치 없이 소프트웨어 수준에서 모든 인터랙션을 구현하기 위한 개발자용 설계서입니다.

---

## 1. 다중 디스플레이 송출 및 합성 아키텍처

기존 하드웨어 믹서 장비(BX-100 등)의 프로토콜 연동 및 리버스 엔지니어링(Reverse Engineering) 비용을 절감하기 위해, **100% 소프트웨어 기반의 브라우저 소스 연동 방식**을 메인 아키텍처로 채택합니다.

### 1.1. OBS Studio 연동 소프트웨어 합성 방식 (최종 권장안)
웹 브라우저의 알파 투명도(Alpha Transparency)와 네트워크 통신 기능을 활용한 무손실 디지털 합성 방식입니다.

* **원리**: 
  1. [frontend/viewer.html](file:///C:/cli-develop/subcast/frontend/viewer.html)을 송출 PC의 OBS 내 **브라우저 소스(Browser Source)**로 등록합니다.
  2. 자막 PC와 송출 PC는 동일한 로컬 네트워크(LAN) 상에 배치합니다.
  3. 자막 PC의 조작기([frontend/presenter.html](file:///C:/cli-develop/subcast/frontend/presenter.html))에서 자막을 변경하면, **웹소켓(Websocket)**을 통해 송출 PC의 OBS 뷰어가 메모리 버퍼 상에서 실시간으로 화면을 부드럽게 페이드(Fade) 전환합니다.
* **장점**: 화면에 불필요한 크롬 창을 띄워 드래그해 둘 필요가 없으며, 물리 믹서 장비 없이 노트북과 PC만으로 최고의 퀄리티를 낼 수 있습니다.

### 1.2. 단순 하드웨어 크로마키(Chroma Key) 대안 모드
만약 현장에 기존 하드웨어 스위처(스플리터, BX-100 등)가 완비되어 있고 TV로 자막을 쏘아야 한다면, 시리얼 제어 없이 오직 **비디오 신호 송출만 단색으로 처리**하여 하드웨어가 스스로 도려내도록 유도합니다.

* **원리**: 뷰어 페이지의 배경색을 투명이 아닌 녹색(`#00FF00`)으로 채운 뒤 이를 두 번째 디스플레이 포트(믹서 입력)로 보내고, 믹서의 수동 크로마키 설정만 켜 둡니다.

---

## 2. 개발자를 위한 시스템 지연 시간 (Latency) 공식

비디오 신호가 카메라 렌즈로부터 캡처되어 합성 및 TV에 출력되기까지의 총 지연 시간($T_{\text{total}}$)은 다음과 같이 결정됩니다.

$$T_{\text{total}} = T_{\text{capture}} + T_{\text{mixing}} + T_{\text{render}}$$

1. **$T_{\text{capture}}$ (비디오 캡처 지연)**: 카메라 HDMI 신호가 캡처 카드를 거쳐 PC 메모리에 적재되는 시간 (약 $30\text{ms} \sim 80\text{ms}$)
2. **$T_{\text{mixing}}$ (소프트웨어 합성 지연)**: OBS가 웹 자막 요소를 비디오 프레임 위에 합성하는 시간 (약 $16\text{ms} \sim 30\text{ms}$)
3. **$T_{\text{render}}$ (출력 디스플레이 렌더링 지연)**: 그래픽 카드가 두 번째 디스플레이 포트(TV)로 프레임을 전송 및 픽셀 출력하는 시간 (약 $8\text{ms} \sim 16\text{ms}$)

* **최종 지연 시간**: 소프트웨어 합성 사용 시 약 **$50\text{ms} \sim 110\text{ms}$** (0.05초~0.11초)의 미세한 딜레이가 발생합니다.
* **현장 적용 평가**: 이 오차는 인체의 시각/청각적 **인지적 보정(Cognitive Compensation)** 범위 내에 있으므로, 예배 설교 및 찬양 가사 전달 환경에서는 싱크 오차를 성도들이 체감하기 어려워 사용에 문제가 없습니다.

---

## 3. 다중 PC 분산 송출 아키텍처 (2-PC Setup)

가장 안정적이고 실제 현장에서 강력히 권장되는 방식인 **역할 분담형 분산 아키텍처(Distributed Architecture)** 세팅 환경의 원리입니다.

```
[자막 PC (Subcast 백엔드)] ──(네트워크/LAN 웹소켓)──> [송출 PC (OBS 브라우저 소스)]
  └─ 자막 제어 (presenter.html)                     ├─ 카메라 영상 수신 (캡처 카드)
  └─ 슬라이드 전환 컨트롤                            ├─ 자막 오버레이 합성
                                                   └─ 스트리밍 및 현장 TV 출력
```

### 3.1. 물리적 장비 다운 시 리스크 분석 (단일 장애점 배제)
* **자막 PC가 다운되는 경우**: 
  송출 PC의 OBS는 계속해서 카메라 영상을 무중단으로 입력받아 처리 중이므로, **화면 송출 및 실시간 중계는 1초의 끊김도 없이 유지**됩니다. 단지 자막의 변경이나 노출만 잠시 멈춥니다.
* **송출 PC가 다운되는 경우**: 
  메인 인코딩 및 스트리밍 시스템이 정지된 것이므로 전체 방송이 멈춥니다.
* **의의**: 자막 PC가 **단일 장애점(SPOF - Single Point of Failure)**이 되지 않으므로 방송 사고 위험이 극도로 최소화됩니다.

---

## 4. 하드웨어 시리얼 제어 배제 사유 및 웹소켓 제어 자동화

### 4.1. 시리얼 통신(RS-422) 배제 사유 (Cost-Benefit)
1. **극심한 개발 공수**: 제조사(보은전자 등)의 비공개 프로토콜 명세 확보가 어려우며, 이를 리버스 엔지니어링하여 헥사 바이트 패킷을 분석하는 작업의 시간 비용이 과다합니다.
2. **유지보수 종속성**: 믹서 장비 모델이 변경되거나 노후화되어 폐기되는 순간, 작성한 시리얼 제어 코드는 완전히 무용지물이 됩니다.
3. **웹소켓(Websocket)으로 완벽 대체 가능**: 웹 페이지 내의 텍스트 전환 및 CSS 모션 그래픽 페이드 효과만으로도 하드웨어 스위처의 동작을 완전하게 소프트웨어적으로 대체 및 상회하는 비주얼을 만들 수 있습니다.

### 4.2. 100% 소프트웨어 웹소켓 제어 흐름
* 엔지니어가 자막 PC에서 다음 장을 누르면, 백엔드 서버를 거쳐 송출 PC의 OBS 브라우저 소스로 신호가 가고, 브라우저가 화면을 갱신하며 자체 CSS 효과(Transition)로 자막을 갱신합니다.

```
[자막 PC 조작기] ──(클릭)──> [Subcast 백엔드(FastAPI)]
                                    │
                             (Websocket 통신)
                                    │
                                    ▼
                      [송출 PC OBS (viewer.html 뷰어)]
                        └─ 웹소켓 이벤트 수신
                        └─ CSS Transition으로 자막 페이드 효과 전환
```

---

## 5. 개발자 핵심 구현 요구사항 및 주의사항 (Checklist)

개발자가 향후 프로그램을 개선하거나 기능을 추가할 때 반드시 인지해야 할 체크리스트입니다.

### 🔒 1. 크로마키 대응용 배경 토글 기능 개발
* **요구사항**: `viewer.html`에 배경색을 동적으로 변경할 수 있는 설정을 마련해야 합니다.
* **주의점**: OBS 브라우저 소스로 쓰일 때는 배경이 투명(`rgba(0, 0, 0, 0)`)해야 하지만, 만약의 경우를 위한 단순 하드웨어 크로마키 입력용으로 쓰일 때는 단색 녹색(`rgb(0, 255, 0)`)이어야 합니다. 컨트롤러(`presenter.html`)에서 이를 제어할 수 있는 웹소켓 신호를 설계해야 합니다.

### 🔒 2. 웹소켓 재연결(Reconnection) 로직 구현
* **요구사항**: 네트워크 순단으로 인해 조작기와 뷰어 간의 연결이 일시적으로 끊어질 수 있습니다.
* **주의점**: 연결이 끊겼을 때 자막 화면이 정지된 상태로 굳어 버리는 현상을 막기 위해, 브라우저가 `onclose` 이벤트를 감지하면 일정 주기마다 재연결을 시도하고 실패 시 강제로 오버레이 화면을 투명화(Opacity 0) 처리하는 페일 세이프(Fail-safe) 로직을 프론트엔드에 추가해야 합니다.

### 🔒 3. 스케일 보정 및 해상도 최적화
* **요구사항**: 서로 다른 해상도의 PC 화면이나 빔 프로젝터 비율에 맞추어 자막 레이아웃이 찌그러지지 않아야 합니다.
* **주의점**: 창 크기가 변경될 때 Fabric.js 캔버스의 반응형 해상도 조절 및 스케일 비율 재조정 공식을 적용하여, 템플릿의 상대적 폰트 크기($vw, vh$)와 개체 위치 비율이 깨지지 않도록 해야 합니다.

---

## 6. 공식 참고 자료 (References)
- [W3C - CSS Transitions Specification](https://www.w3.org/TR/css-transitions-1/)
- [OBS Studio Developer - Source & Transition API](https://obsproject.com/docs/)
- [ITU-R BT.1359-1: Audio-to-Video Synchronization Specification](https://www.itu.int/rec/R-REC-BT.1359-1-199811-I/en)
- [Wikipedia - Single point of failure](https://en.wikipedia.org/wiki/Single_point_of_failure)
