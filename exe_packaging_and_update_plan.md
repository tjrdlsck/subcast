# Subcast - Windows 배포 및 원클릭 자동 업데이트 기획서

## 1. 개요 및 목표
* **목표**: 현재의 FastAPI 기반 백엔드와 HTML/JS 기반 프론트엔드로 구성된 웹 애플리케이션을 단일/독립 실행 가능한 Windows `.exe` 파일로 패키징.
* **주요 기능**: 사용자 편의성을 위해 프론트엔드 UI 내에서 **'버튼 한 번(One-Click)'**으로 최신 버전으로 자동 업데이트되는 기능 구현.
* **타겟 환경**: Windows 10 및 Windows 11.

---

## 2. 현재 아키텍처 분석
* **백엔드**: Python, FastAPI (`backend/main.py`)
* **프론트엔드**: 정적 HTML/CSS/JS (`frontend/` 폴더)
* **데이터 및 설정**: SQLite 데이터베이스 (`GAE_Bible.db` 등) 및 외부 리소스
* **특이사항**: Node.js 기반의 데스크톱 프레임워크(Electron 등)가 없으므로, Python 프로젝트를 `PyInstaller`를 사용해 컴파일하고, 백그라운드 서버로 띄운 뒤 사용자가 웹 브라우저를 통해 접근하게 하거나 데스크톱 앱과 같은 UX를 제공해야 함.

---

## 3. EXE 패키징 (빌드) 전략
Python 환경을 윈도우 실행 파일로 변환하기 위해 **PyInstaller**를 사용합니다.

1. **진입점 스크립트 (`run.py`) 생성**
   * 프로젝트 루트에 Uvicorn 서버를 프로그래밍 방식으로 실행하는 `run.py` 작성.
   * 개발 환경과 빌드 환경(`sys._MEIPASS`)을 구분하여 정적 파일(`frontend` 폴더)의 경로를 동적으로 인식하도록 처리.
2. **PyInstaller `.spec` 파일 구성**
   * `frontend`, `data`, `.db` 등의 필수 데이터 폴더를 빌드 산출물에 포함(Data Files).
   * 콘솔 창 숨기기(`--noconsole` 또는 `--windowed`) 옵션을 통해 백그라운드에서 깔끔하게 실행되도록 설정.
   * 애플리케이션 아이콘(`.ico`) 적용.
3. **브라우저 자동 실행 로직 (선택사항)**
   * 서버가 시작되면 내장 모듈(`webbrowser.open`)을 사용해 사용자 브라우저 창(예: `http://localhost:8000`)이 자동으로 열리도록 구성하여 데스크톱 앱과 같은 UX 제공.

---

## 4. 원클릭 자동 업데이트 (Auto-Update) 구현 아키텍처
현재 런타임 자체를 업데이트해야 하므로, 실행 중인 `.exe` 파일이 자기 자신을 덮어쓸 수 없는 Windows의 OS 제약을 우회하는 구조가 필요합니다.

### 4.1. 업데이트 배포 서버 연동
* **GitHub Releases 활용 (권장)**: 릴리스 태그를 통해 버전을 관리하고 새 빌드 압축파일(`.zip`)을 업로드. API를 통해 최신 버전을 체크.
* 혹은 자체 운영 중인 서버(AWS S3 등)를 통한 버전 정보(json) 및 파일 제공.

### 4.2. 동작 프로세스 (로직)
1. **버전 체크**: FastAPI 백엔드가 시작될 때 또는 브라우저 접속 시 주기적으로 배포 서버의 `version.json`을 확인.
2. **업데이트 알림**: 프론트엔드 UI 상단 또는 설정 영역에 "새로운 버전이 있습니다" 알림과 함께 **[업데이트 하기]** 버튼 활성화.
3. **버튼 클릭 시 처리 (다운로드)**:
   * 백엔드 API (`POST /api/update`) 호출.
   * 백엔드는 최신 압축 파일(`.zip`)을 임시 폴더(Temp)로 다운로드 및 압축 해제.
4. **업데이터 실행 및 파일 교체 (중요)**:
   * 프로그램 파일의 잠금(Lock)을 피하기 위해, 작고 가벼운 별도의 `updater.exe` (혹은 배치 스크립트 `.bat`)를 생성하여 임시 폴더로 복사 및 독립 프로세스로 실행.
   * 기존 메인 프로그램(`subcast.exe`) 스스로 종료 (`sys.exit()`).
   * `updater.exe`는 기존 프로세스가 종료될 때까지 1~2초 대기한 후, 압축 해제한 새 파일들로 원본 파일들을 덮어쓰기 진행.
5. **자동 재시작**:
   * 교체가 완료되면 `updater.exe`가 다시 메인 프로그램(`subcast.exe`)을 실행시키고 스스로 종료.
   * 브라우저 프론트엔드에서는 지속적으로 서버에 Ping을 날려 연결이 복구되면 페이지를 새로고침하여 업데이트 완료를 사용자에게 알림.

---

## 5. 단계별 수정 및 구현 계획 (작업 지시서)

### [Phase 1] 기반 마련 및 EXE 패키징
* [ ] 의존성 추가: `pip install pyinstaller requests`
* [ ] 패키징 진입점 분리: `run.py` 생성 및 경로 탐색 로직(PyInstaller 임시 경로 매핑) 추가.
* [ ] `.spec` 빌드 설정 파일 작성 및 정적 리소스 복사 로직 작성.
* [ ] `webbrowser` 모듈을 이용한 초기 브라우저 자동 시작 로직 추가.

### [Phase 2] 업데이터 (Updater) 모듈 개발
* [ ] `updater.py` 작성: 프로세스 종료 대기 -> 최신 파일 복사/덮어쓰기 -> 메인 프로그램 재시작 기능을 담당.
* [ ] `updater.py`를 단일 파일로 패키징하여 메인 프로젝트에 병합할 수 있도록 준비.

### [Phase 3] 백엔드 업데이트 API 구현
* [ ] 프로젝트 루트에 현재 버전을 기록할 `version.json` 또는 환경 변수 설정.
* [ ] `GET /api/version` 엔드포인트: 현재 구동 중인 버전과 외부 배포 서버의 최신 버전 비교값 반환.
* [ ] `POST /api/update` 엔드포인트: 업데이트 파일 다운로드, 압축 해제, `updater.exe` 백그라운드 호출 및 앱 종료 처리.

### [Phase 4] 프론트엔드 UI 연동
* [ ] 사용자 UI(예: `index.html`, `presenter.html` 등) 내에 버전 정보 노출.
* [ ] 최신 버전 발견 시 '업데이트 아이콘'이나 모달 팝업 표시.
* [ ] 업데이트 진행 시 다운로드 프로그레스바를 제공하거나, 서버 재시작 동안 스피너(Loading)를 보여주어 사용자 불안감 해소.

---
*본 수정계획서를 검토하신 후 승인(컨펌)해 주시면, [Phase 1]부터 순차적으로 코드를 생성 및 수정해 나가겠습니다.*
