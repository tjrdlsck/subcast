# 프론트엔드 보안 및 성능 개선(리팩토링) 기획서

본 문서는 Subcast 프로젝트의 코드 리뷰에서 식별되었으나, 기존 기능 보존 원칙에 따라 즉각적인 반영이 보류된 **프론트엔드(UI/UX) 핵심 이슈**들에 대한 상세 원인 분석 및 전문 기획/아키텍트 관점의 해결 방안을 담고 있습니다.

---

## 1. 보류 사유 요약
현재 프론트엔드(`editor.html`, `presenter.html`, `viewer.html`)는 DOM 요소의 강제 갱신과 이벤트 리스너를 통해 모든 UI 상태를 제어하고 있습니다. 이러한 구조에서 `innerHTML`을 무분별하게 변경하거나 이벤트 리스너 구조를 섣불리 뜯어고칠 경우, **기존의 드래그 앤 드롭 동작, 슬라이드 선택 기능, 그리고 Fabric.js 캔버스와의 동기화가 완전히 망가질 위험**이 매우 높았습니다. 따라서 기능 안정성을 100% 보장하기 위해 별도의 단계적 리팩토링 과제로 분리하였습니다.

---

## 2. 주요 보류 이슈 상세 분석

### 이슈 A. `innerHTML` 남용으로 인한 XSS(크로스 사이트 스크립팅) 보안 취약점
*   **현재 상태:** 
    *   사용자가 입력한 "찬양 가사", "슬라이드 제목", "편집자 이름" 등의 텍스트 데이터가 `innerHTML` 속성을 통해 브라우저 DOM에 직접 렌더링되고 있습니다. (예: `editor.html`의 슬라이드 썸네일 리스트, 가사 분해 미리보기 등)
*   **문제의 원인 (Why it's critical):**
    *   사용자가 악의적인 자바스크립트 코드(예: `<script>alert('hacked')</script>`)나 비정상적인 HTML 태그를 입력 항목에 넣을 경우, 이를 열어보는 다른 편집자나 뷰어의 브라우저에서 해당 스크립트가 그대로 실행됩니다.
    *   이는 세션 탈취, 화면 변조, 클라이언트 측 성능 저하를 일으킬 수 있는 치명적인 웹 보안 결함입니다.

### 이슈 B. 비효율적 전체 DOM 렌더링 및 이벤트 리스너 중복 등록 (성능 저하)
*   **현재 상태:**
    *   슬라이드가 하나 추가/삭제되거나 상태가 변경될 때마다, 좌측 슬라이드 목록 창의 HTML 전체를 `""`(빈 문자열)로 초기화한 뒤 처음부터 다시 그립니다(`renderSlides` 등).
    *   이 과정에서 드래그 앤 드롭, 클릭 이벤트를 매번 새로 등록(`addEventListener`)하고 있습니다.
*   **문제의 원인 (Why it's critical):**
    *   슬라이드가 50~100장 단위로 넘어가면, 글씨 하나를 바꿀 때마다 100개의 썸네일 DOM 요소가 파괴되었다가 다시 생성됩니다. 브라우저 렌더링(Reflow/Repaint)에 엄청난 부하가 걸리며 에디터 화면이 멈추거나 뚝뚝 끊기는(Freeze) 현상이 발생합니다.
    *   또한 이벤트 리스너가 제대로 해제되지 않고 계속 누적되면, 메모리 누수(Memory Leak)가 발생해 브라우저가 강제 종료될 수 있습니다.

---

## 3. 기획 및 아키텍처 관점의 해결 방안 (Action Plan)

안정적인 프로덕트를 만들기 위해 단순한 '땜질식 수정'이 아닌, **체계적이고 구조적인 리팩토링**을 다음과 같은 3단계로 기획합니다.

### Phase 1: 보안 필터링 레이어 도입 (가장 빠르고 안전한 방안)
전체 구조를 유지하면서 XSS 공격만 차단하기 위한 과도기적 조치입니다.
*   **해결책:** 클라이언트 측 렌더링에 데이터를 주입하기 직전, 모든 텍스트 데이터를 안전하게 치환(Sanitize)하는 유틸리티 함수를 전역에 도입합니다.
*   **구현 방향:**
    ```javascript
    // 모든 텍스트 출력을 이 함수를 거치도록 변경
    function escapeHTML(str) {
        if (!str) return '';
        return String(str).replace(/[&<>'"]/g, match => ({
            '&': '&amp;', '<': '&lt;', '>': '&gt;', "'": '&#39;', '"': '&quot;'
        })[match]);
    }
    // 예시: item.innerHTML = `<div>${escapeHTML(slide.title)}</div>`;
    ```

### Phase 2: 이벤트 위임(Event Delegation) 패턴 적용
이벤트 리스너 중복 할당과 메모리 누수를 해결하기 위한 구조 개선입니다.
*   **해결책:** 각 슬라이드 아이템(`div.slide-item`)마다 수십 개의 이벤트를 매번 다는 방식을 폐기합니다.
*   **구현 방향:** 슬라이드들을 감싸고 있는 **'부모 컨테이너(List Wrapper)'에 단 하나의 이벤트 리스너만 등록**합니다. 클릭이나 드래그 이벤트가 발생하면 `e.target`을 추적하여 어느 슬라이드에서 발생한 이벤트인지 판별(Delegation)하여 처리합니다. 이로써 메모리 사용량을 획기적으로 줄일 수 있습니다.

### Phase 3: DOM 부분 업데이트 및 상태 관리(State Management) 고도화
장기적인 관점에서 대규모 데이터를 원활히 처리하기 위한 UI 렌더링 구조 개편입니다.
*   **해결책:** `innerHTML = ""`로 전체를 밀어버리는 방식을 버리고, **"변경된 슬라이드만 찾아 부분적으로 갱신"**하는 논리를 작성합니다.
*   **구현 방향:**
    *   Vanilla JS 환경을 유지한다면: `DocumentFragment`를 활용하여 DOM 조작을 메모리 상에서 먼저 끝내고 한 번에 화면에 붙이거나, 기존 DOM 노드와 비교(Diffing)하여 텍스트/이미지만 교체하는 함수 구축.
    *   장기 프레임워크 도입 고려: 본 프로젝트가 지속 성장한다면, 이러한 상태 변화에 따른 UI 렌더링을 자동으로 최적화해 주는 React, Vue 등의 프레임워크로 뷰 단을 점진적 마이그레이션하는 것을 적극 추천합니다.

---

## 4. 진행 권고사항
위 작업은 **에디터 UI의 뼈대를 건드리는 작업**입니다. 따라서 기능 수정을 진행하시려면,
1. 반드시 새로운 브랜치(`refactor/frontend-ui`)를 생성하여 작업해야 합니다.
2. Phase 1(보안) -> Phase 2(이벤트) -> Phase 3(렌더링) 순으로 점진적으로 반영하고, 매 단계마다 "슬라이드 선택, 드래그 순서 변경, 캔버스 동기화" 기능이 정상 작동하는지 QA를 거쳐야 합니다.
